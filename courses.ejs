<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Courses</title>
   <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
   <h1>Courses</h1>
   <form action="/courses/add" method="POST">
      <label for="courseCode">Course Code:</label>
      <input type="text" id="courseCode" name="courseCode" required><br>
      <label for="courseName">Course Name:</label>
      <input type="text" id="courseName" name="courseName" required><br>
      <label for="instructor">Instructor:</label>
      <input type="text" id="instructor" name="instructor" required><br>
      <button type="submit">Add Course</button>
   </form>

   <ul>
      <% courses.forEach(course=> { %>
         <li>
            <%= course.courseCode %> - <%= course.courseName %> (Instructor: <%= course.instructor %>)
                     <form action="/courses/update/<%= course._id %>" method="POST">
                        <input type="text" name="courseName" placeholder="New Course Name">
                        <button type="submit">Update</button>
                     </form>
                     <form action="/courses/delete/<%= course._id %>" method="POST">
                        <button type="submit">Delete</button>
                     </form>

         </li>
         <% }) %>
   </ul>

   <a href="/">Go Back</a>
</body>

</html>